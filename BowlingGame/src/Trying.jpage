String t = "[1,2][6,1]";
String[] strFrames = t.split("]");
Array<int> frames = new Array<int>();
for(String frame : strFrames) {
	frame = frame.substring(1);
	for(String framePart : frame.split(",")) {
		frames.add(Integer.parseInt(framePart));
	}
	
}
frames